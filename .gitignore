/node_modules
/public/hot
/public/storage
/storage/*.key
/vendor
.env
.env.backup
.phpunit.result.cache
docker-compose.override.yml
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/.idea
/.vscode
# Локальный portable Redis (бинарники Windows). На проде ставится из пакетов.
/.redis
# Локальный portable nginx (бинарники Windows). На проде ставится из пакетов.
/.nginx
# Артефакты сборки от прошлых .ini правок
*.ini.bak.*
# Бэкапы Postgres (bind-mount из docker-compose backup-сервиса).
# Внутри лежат .sql.gz — на сервере сами по себе ценные, но в git не место.
/backups
